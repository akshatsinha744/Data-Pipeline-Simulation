# Data-Pipeline-Simulation
Design a simple ETL (Extract, Transform, Load) process using Python. Extract data from a JSON file, transform it by normalizing nested structures into a flat table, and load the transformed data into a Postgresql database
